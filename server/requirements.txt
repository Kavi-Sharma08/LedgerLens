fastapi>=0.115
uvicorn[standard]>=0.30
motor>=3.4
pydantic>=2.7
pydantic-settings>=2.2

# Authentication (OAuth, credentials, sessions, password hashing) lives in
# Next.js/Auth.js — the API performs none of it.
